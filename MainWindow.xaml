<Window x:Class="EpochAddonUpdater.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Project Epoch Addon Updater"
        Width="1280" Height="720" MinWidth="900" MinHeight="560"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="CanResize"
        Icon="Assets/epoch-icon.ico"
        Background="#0B0908" Foreground="#F4F0EA"
        FontFamily="Segoe UI"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0"
                            ResizeBorderThickness="6"
                            GlassFrameThickness="0"
                            CornerRadius="0"
                            UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Window.Resources>
        <SolidColorBrush x:Key="PanelBrush" Color="#CC100E0D"/>
        <SolidColorBrush x:Key="PanelStrongBrush" Color="#E6110F0E"/>
        <SolidColorBrush x:Key="LineBrush" Color="#2B2928"/>
        <SolidColorBrush x:Key="MutedBrush" Color="#9B9A9A"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#FF9146"/>
        <SolidColorBrush x:Key="WarnBrush" Color="#F0CC17"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#FF3939"/>
        <SolidColorBrush x:Key="GoodBrush" Color="#A8CE3A"/>

        <Style TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#F4F0EA"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.86"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.72"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style x:Key="WindowButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#D8D2CB"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style TargetType="ToggleButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#F4F0EA"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.86"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.72"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="InputBox" TargetType="TextBox">
            <Setter Property="Background" Value="#181411"/>
            <Setter Property="Foreground" Value="#F4F0EA"/>
            <Setter Property="BorderBrush" Value="#58514C"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="Padding" Value="12,7,38,7"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#BDB8B2"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Margin" Value="0,8,0,0"/>
        </Style>
        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="#D8D2CB"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Margin" Value="0,8,18,0"/>
        </Style>
        <Style x:Key="DarkComboBoxItem" TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#15110F"/>
            <Setter Property="Foreground" Value="#F4F0EA"/>
            <Setter Property="Padding" Value="12,7"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="ItemRoot" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="ItemRoot" Property="Background" Value="#2A211B"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemRoot" Property="Background" Value="#35271E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="DarkComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="#171210"/>
            <Setter Property="Foreground" Value="#F4F0EA"/>
            <Setter Property="BorderBrush" Value="#34302D"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="MaxDropDownHeight" Value="220"/>
            <Setter Property="ItemContainerStyle" Value="{StaticResource DarkComboBoxItem}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton x:Name="Toggle"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}"
                                          Padding="{TemplateBinding Padding}">
                                <Grid>
                                    <ContentPresenter HorizontalAlignment="Left"
                                                      VerticalAlignment="Center"
                                                      Content="{TemplateBinding SelectionBoxItem}"
                                                      Margin="0,0,28,0"/>
                                    <TextBlock Text="v" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="#A9A39D" FontSize="14"/>
                                </Grid>
                            </ToggleButton>
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True"
                                   Focusable="False"
                                   PopupAnimation="Fade">
                                <Border Background="#15110F" BorderBrush="#34302D" BorderThickness="1" MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ScrollViewer>
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="AddonListBoxStyle" TargetType="ListBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#F4F0EA"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
            <Setter Property="VirtualizingStackPanel.IsVirtualizing" Value="True"/>
            <Setter Property="VirtualizingStackPanel.VirtualizationMode" Value="Recycling"/>
        </Style>
        <Style TargetType="ScrollViewer">
            <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
        </Style>
        <Style TargetType="Thumb">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border Background="#706B65" CornerRadius="2" Margin="4,2"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ScrollBar">
            <Setter Property="Width" Value="10"/>
            <Setter Property="Background" Value="#0D0B0A"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="{TemplateBinding Background}">
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageUpCommand" Opacity="0" Height="0"/>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb/>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.PageDownCommand" Opacity="0" Height="0"/>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="AddonListBoxItemStyle" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Root" Background="{TemplateBinding Background}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#241B20"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="#2D2333"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <DataTemplate x:Key="AddonRowTemplate">
            <Grid>
            <Border x:Name="SectionHeader" Visibility="Collapsed" Height="54" Margin="20,0,16,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="v" FontSize="14" Margin="0,0,12,0"/>
                    <TextBlock Text="{Binding HeaderText}" Foreground="{StaticResource AccentBrush}" FontSize="18" FontWeight="SemiBold"/>
                </StackPanel>
            </Border>
            <Border x:Name="AuthorHeader" Visibility="Collapsed" Height="36" Margin="48,0,16,0">
                <TextBlock Text="{Binding HeaderText}" Foreground="#BDB8B2" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
            </Border>
            <Border x:Name="AddonRow" BorderBrush="#181615" BorderThickness="0,0,0,1">
                <Grid Height="46" Margin="20,0,16,0" ClipToBounds="True">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="28"/>
                        <ColumnDefinition Width="2*" MinWidth="120"/>
                        <ColumnDefinition Width="4*" MinWidth="0"/>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="44"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="{Binding IconText}" Foreground="{Binding IconBrush}" FontWeight="Bold" FontSize="13" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1" Text="{Binding Title}" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap"/>
                    <TextBlock Grid.Column="2" Text="{Binding ShortDescription}" FontSize="12" Foreground="#928F8D" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap"/>
                    <Button Grid.Column="3"
                            x:Name="AddonActionButton"
                            Foreground="{Binding ActionBrush}"
                            FontWeight="{Binding ActionWeight}"
                            FontSize="12"
                            HorizontalContentAlignment="Right"
                            Padding="2,8"
                            Click="AddonAction_Click">
                        <TextBlock Text="{Binding ActionText}" FontFamily="{Binding ActionFontFamily}" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap"/>
                    </Button>
                    <Button Grid.Column="4"
                            x:Name="DeleteButton"
                            Content="&#xE74D;"
                            FontFamily="Segoe MDL2 Assets"
                            Foreground="#A82525"
                            FontSize="13"
                            Width="30"
                            Height="30"
                            Padding="0"
                            HorizontalAlignment="Center"
                            HorizontalContentAlignment="Center"
                            Click="DeleteAddon_Click"/>
                </Grid>
            </Border>
            </Grid>
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding RowKind}" Value="SectionHeader">
                    <Setter TargetName="SectionHeader" Property="Visibility" Value="Visible"/>
                    <Setter TargetName="AddonRow" Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding RowKind}" Value="AuthorHeader">
                    <Setter TargetName="AuthorHeader" Property="Visibility" Value="Visible"/>
                    <Setter TargetName="AddonRow" Property="Visibility" Value="Collapsed"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding IsRecommended}" Value="True">
                    <Setter TargetName="DeleteButton" Property="Visibility" Value="Collapsed"/>
                    <Setter TargetName="AddonActionButton" Property="Foreground" Value="#D9DD51"/>
                </DataTrigger>
            </DataTemplate.Triggers>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.Background>
            <ImageBrush x:Name="LauncherBackgroundBrush" ImageSource="Assets/LauncherBackgrounds/launcher-background-01.webp" Stretch="UniformToFill"/>
        </Grid.Background>

        <Border Background="#70000000"/>
        <Border Margin="10" BorderBrush="#43413F" BorderThickness="1" CornerRadius="7" Background="#A0060505"/>
        <Border x:Name="DragSurface" Height="40" VerticalAlignment="Top" Background="Transparent" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,8,16,0">
            <Button Content="&#xE713;" Style="{StaticResource WindowButton}" Click="OpenSettings_Click"/>
            <Button Content="&#xE921;" Style="{StaticResource WindowButton}" Click="Minimize_Click"/>
            <Button x:Name="MaximizeButton" Content="&#xE922;" Style="{StaticResource WindowButton}" Click="Maximize_Click"/>
            <Button Content="&#xE8BB;" Style="{StaticResource WindowButton}" Click="Close_Click"/>
        </StackPanel>
        <Grid Margin="42,24,42,24">
            <Grid.RowDefinitions>
                <RowDefinition Height="76"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="94"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="280"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="120"/>
                </Grid.ColumnDefinitions>
                <Image Source="Assets/epoch-logo.png" Width="225" HorizontalAlignment="Left" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="ADDONS" Foreground="#CADB4D" FontSize="18" FontWeight="SemiBold" Margin="0,0,8,0"/>
                    <Border x:Name="UpdateBadge" Background="#D85D45" CornerRadius="9" MinWidth="18" Height="18" Visibility="Collapsed" VerticalAlignment="Top">
                        <TextBlock x:Name="UpdateBadgeText" Text="0" Foreground="White" FontSize="11" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </StackPanel>
            </Grid>

            <Border Grid.Row="1" Background="{StaticResource PanelBrush}" BorderBrush="{StaticResource LineBrush}" BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="70"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="64"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0" Margin="28,0,20,0">
                        <Grid Width="260" HorizontalAlignment="Right" VerticalAlignment="Center">
                            <TextBox x:Name="SearchBox" Style="{StaticResource InputBox}" TextChanged="SearchBox_TextChanged"/>
                            <TextBlock Text="&#xE721;" FontFamily="Segoe MDL2 Assets" FontSize="20" Foreground="#F4F0EA" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,11,0" IsHitTestVisible="False"/>
                        </Grid>
                    </Grid>

                    <ListBox x:Name="AddonList"
                             Grid.Row="1"
                             Style="{StaticResource AddonListBoxStyle}"
                             ItemContainerStyle="{StaticResource AddonListBoxItemStyle}"
                             ItemTemplate="{StaticResource AddonRowTemplate}"
                             SelectionChanged="AddonList_SelectionChanged"/>

                    <Border Grid.Row="2" BorderBrush="{StaticResource LineBrush}" BorderThickness="0,1,0,0">
                        <Grid Margin="24,0">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Button x:Name="CheckUpdatesButton" Foreground="#929292" FontSize="13" Click="CheckUpdates_Click">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="&#xE72C;" FontFamily="Segoe MDL2 Assets" FontSize="15" Margin="0,0,8,0"/>
                                        <TextBlock Text="Check for updates"/>
                                    </StackPanel>
                                </Button>
                                <Button x:Name="AddAddonButton" Style="{StaticResource AccentButton}" FontSize="13" Click="AddAddon_Click">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="+" FontSize="17" Margin="0,0,8,0"/>
                                        <TextBlock Text="Add new addon"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                            <Button x:Name="UpdateAllButton" HorizontalAlignment="Right" VerticalAlignment="Center" Click="UpdateAll_Click"/>
                        </Grid>
                    </Border>
                </Grid>
            </Border>

            <Grid Grid.Row="2">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock x:Name="FooterStatus" Text="Scanning addons..." FontSize="15" FontWeight="SemiBold"/>
                    <TextBlock x:Name="FooterSubStatus" Text="" FontSize="13" Foreground="#9B9A9A" Margin="0,5,0,0" Visibility="Collapsed"/>
                    <ProgressBar x:Name="Progress" Height="5" Margin="0,12,180,0" Minimum="0" Maximum="100" Value="0" Foreground="#F3A12D" Background="#211E1B"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,156,18">
                    <Button x:Name="RetryButton" FontSize="15" FontWeight="SemiBold" Visibility="Collapsed" Click="Retry_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE72C;" FontFamily="Segoe MDL2 Assets" FontSize="18" Margin="0,0,8,0"/>
                            <TextBlock Text="Retry"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
                <Button x:Name="PlayButton" Content="PLAY" HorizontalAlignment="Right" VerticalAlignment="Center" Width="128" Height="58" FontSize="20" FontWeight="Bold" Background="#3E6E20" Foreground="#D8F06E" Click="Play_Click"/>
                <Button x:Name="VersionButton" HorizontalAlignment="Right" VerticalAlignment="Bottom" FontSize="12" Foreground="#8F8D8B" Padding="4,0" Click="VersionButton_Click"/>
            </Grid>
        </Grid>

        <Grid x:Name="Overlay" Visibility="Collapsed" Background="#AA000000"/>
    </Grid>
</Window>

